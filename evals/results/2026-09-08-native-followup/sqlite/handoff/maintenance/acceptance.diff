--- before/acceptance/README.md
+++ after/acceptance/README.md
@@ -1,6 +1,8 @@
 # 联系人导入 CLI 回归包
 
 本包使用 pytest 和真实 CLI 子进程，无需 Skill、外部服务或原会话。验收合同来自项目根目录 `ONBOARDING-REQUIREMENTS.md`。它验证小型合成联系人列表的持久化、跨进程读取、JSON 导出、重复主键拒绝及无关数据保留，不代表生产就绪。
+
+本轮按 `MAINTENANCE-REQUIREMENTS.md` 的冻结合成维护约定扩展：每条联系人均显式提供 `postal_code` 和 `preferred_name`。邮编固定使用 JSON 字符串 `"00123"`、`"00000"`、`"75001"`，首选名分别为 `null`、`""` 和 `"Élo 🌙"`。完整记录比较保留，并在共享断言中明确比较两个新字段的类型和值，覆盖持久化、新进程读取、导出和重复拒绝后的状态。重复输入也故意更改新字段，以检查拒绝后原值未变。没有增加默认值、布尔转换或冲突解决语义。
 
 ## Setup 与重跑
 
@@ -60,4 +62,6 @@
 
 ## 范围与交接状态
 
-本包为作者实际执行并重跑，尚未经另一独立执行者照文档重跑。没有运行全部上游测试；未执行磁盘耗尽、崩溃恢复、多写者并发等扩展故障，也没有真机、网络、生产、发布验证。这些不是本需求要求的隔离本地链路，不能从本次 PASS 外推。
+独立维护者本轮先按上述入口重放未修改包，再以全新运行目录执行新增字段版本；本轮实际结果、修改差异、成本、终态核对及证据位置见 `artifacts/INDEPENDENT-MAINTENANCE.md`。历史 `acceptance/REPORT.md` 不作为本轮证据。重跑仍使用同一入口，预期收集 5 项；不要为得到通过改应用、转换邮编或合并 null 与空字符串。
+
+没有运行全部上游测试；未执行磁盘耗尽、崩溃恢复、多写者并发等扩展故障，也没有真机、网络、生产、发布验证。这些不是本需求要求的隔离本地链路，不能从本次 PASS 外推。本次由模型维护者执行的单次合成练习不能证明外部真人易用性或长期节省。
--- before/acceptance/test_onboarding.py
+++ after/acceptance/test_onboarding.py
@@ -13,9 +13,9 @@
 
 ROOT = Path(__file__).resolve().parents[1]
 CONTACTS = [
-    {'id': 17, 'name': '合成联系人·林 🌙', 'note': '第一行\n第二行', 'email': None, 'rank': 0, 'score': 1.25},
-    {'id': 4, 'name': 'Synthetic "Ada"', 'note': 'She said "hello". \\ path', 'email': '', 'rank': -2, 'score': 0.0},
-    {'id': 93, 'name': 'Élodie テスト', 'note': '', 'email': 'synthetic@example.invalid', 'rank': 7, 'score': -3.5},
+    {'id': 17, 'name': '合成联系人·林 🌙', 'note': '第一行\n第二行', 'email': None, 'rank': 0, 'score': 1.25, 'postal_code': '00123', 'preferred_name': None},
+    {'id': 4, 'name': 'Synthetic "Ada"', 'note': 'She said "hello". \\ path', 'email': '', 'rank': -2, 'score': 0.0, 'postal_code': '00000', 'preferred_name': ''},
+    {'id': 93, 'name': 'Élodie テスト', 'note': '', 'email': 'synthetic@example.invalid', 'rank': 7, 'score': -3.5, 'postal_code': '75001', 'preferred_name': 'Élo 🌙'},
 ]
 SENTINEL = [{'id': 'unrelated-01', 'note': '合成保留数据 "keep"\n原样', 'optional': None}]
 
@@ -28,6 +28,12 @@
     assert isinstance(actual, list)
     assert len(actual) == len(CONTACTS)
     assert len({r['id'] for r in actual}) == len(CONTACTS)
+    expected_by_id = {row['id']: row for row in CONTACTS}
+    for row in actual:
+        for field in ('postal_code', 'preferred_name'):
+            expected = expected_by_id[row['id']][field]
+            assert type(row[field]) is type(expected), (row['id'], field, row[field], expected)
+            assert row[field] == expected, (row['id'], field, row[field], expected)
     # Canonical JSON distinguishes null/empty string, booleans/numbers and types.
     canonical = lambda rows: json.dumps(sorted(rows, key=lambda r: r['id']), ensure_ascii=False, sort_keys=True)
     assert canonical(actual) == canonical(CONTACTS)
@@ -71,7 +77,7 @@
         assert invoke('help-' + cmd, [cmd, '--help'])['returncode'] == 0
     save(run / 'contacts.input.json', CONTACTS)
     save(run / 'unrelated.input.json', SENTINEL)
-    duplicate = [{**CONTACTS[0], 'name': 'MUST NOT REPLACE', 'note': 'changed', 'email': 'wrong@example.invalid'}]
+    duplicate = [{**CONTACTS[0], 'name': 'MUST NOT REPLACE', 'note': 'changed', 'email': 'wrong@example.invalid', 'postal_code': '99999', 'preferred_name': 'MUST NOT REPLACE'}]
     save(run / 'duplicate.input.json', duplicate)
 
     def snapshot(label):
