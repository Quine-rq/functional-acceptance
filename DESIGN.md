# 功能验收 Skill：产品与架构设计提案

版本：设计稿 v0.2 · 2026-09-08。仓库名称：`functional-acceptance`。此版本号只表示设计修订，不是可安装产品的发行版本。

**历史状态：下文保留 v0.2 设计基线形成时的提案与未验证假设。当前已开始 M1 受控本地 CLI/CSV 实现，实际能力与运行说明以 [README.md](README.md) 为准；不能把设计中的后续能力视为已实现。产品定位不限定 Flutter/Android。**

## 阅读导航

- 本文：产品定义、突出能力、使用体验、范围与开发顺序。
- [架构与执行约定](ARCHITECTURE.md)：数据、Module / Interface、判定、恢复和安全。
- [验证与发布门槛](VALIDATION.md)：怎样证明 Skill 有用且不会虚假通过。
- [领域语言](CONTEXT.md)：区分用户流程、验收要求、观察、证据和判定。
- [依据与设计取舍](DECISIONS.md)：一手来源、三种方案比较、尚未验证的假设。

## 1. 把定义打透

### 一句话定义

**功能验收 Skill 是交给现有 Agent、面向不同技术栈的可执行验收方法：围绕用户应得到的结果，确认验收标准，调用项目已有工具实际验证，交付可核查结论，并留下下一次能重跑的回归资产。**

短表达：**验收用户结果，而不只是检查动作成功。**

这里有四个不可少的限定：

1. **有目标来源**：先保留原始验收目标，再判断可执行范围；业务应该怎样由需求及负责人确认，现有代码只帮助了解当前实现。
2. **有实际观察**：读代码、列清单、测试程序返回零，都不自动等于用户流程成立。
3. **有范围约束**：结论只覆盖指定运行对象、环境、适用角色、场景和证据能支持的要求；原请求中尚未完成核查的目标仍须显式保留。
4. **有复用结果**：完成一次后保留验收约定和执行条件，下次不用重新口头解释整条流程。

它不是独立通用 Agent、自动发版平台、全自动安全审计、单纯提示词合集，也不以“修到测试变绿”为默认任务。发现问题先交付证据与复现，修改业务代码、发布和外部评论仍需各自授权。

### 用户真正购买的结果

用户改完一个功能后，少花时间亲自试、逐项追问和组织证据，同时清楚知道：哪些结果成立、哪里失败、哪里仍不知道、接下来最小需要补什么。

不是承诺“保证产品没有 Bug”，也不是自动替负责人批准上线。

## 2. 动手前五问

| 问题 | 本次设计的回答 |
| --- | --- |
| 谁的问题？ | 第一批服务使用 AI 维护软件产品的独立开发者和小团队；他们有获授权的测试目标和现成工具，但仍反复亲自补测通过界面、接口或命令完成的用户流程。 |
| 目标与当前真相？ | 目标是技术栈无关的功能验收 Skill，业务领域不限定 IM；目前只有设计和资料核查，没有可运行产品与外部使用证据。具体样本、执行组合及其支持范围仍须验证。 |
| 什么不能破坏？ | 业务规则、权限、数据归属、现有测试语义、工作区已有改动、用户的发布审批与真实环境安全。 |
| 链路怎样闭环？ | 保留原始目标→按意图形成计划或检查执行能力→验收约定→准备与执行→观察判定→结束处置与交接→回归包。计划模式有独立退出条件，详见第 4、5 节。 |
| 怎样算成功？ | 同样环境和任务下，相比现有 Agent 加测试工具组合，人工劳动确实减少；已知缺陷被检出；证据不足时不通过；换项目能复用，外部开发者愿意再次用。 |

### 第一批用户与非目标用户

优先：有获授权测试环境、有明确业务负责人、能使用现成执行与观察工具的软件团队。项目已有自动化测试时收益应更快出现；缺少工具时也能得到约定与接入缺口，但不能假装完成自动验收。是否能够实际运行取决于本次所需能力，不以语言、框架或是否有手机作为产品入口条件。

暂不优先：没有测试目标或授权、只有一句模糊创意的人；希望随意操作线上产品就得到发布保证的人；希望替代全部 QA、渗透测试或硬件实验室的团队。

## 3. 三个核心亮点，两个增强能力

以下是拟议产品能力，不宣称行业首创。价值须通过实际对照实验建立。

### 核心一：结果反证——专门检查“表面成功”

为关键验收要求选一个低成本、直接相关的验证动作：如果实现只是看起来成功，怎样最容易暴露它？

| 表面成功 | 补充的验证动作 | 能支持的结论与限制 |
| --- | --- | --- |
| 收藏图标变亮 | 按约定结束并重新打开测试 App，或采用能排除原内存状态的独立观察 | 页面往返可能仍读同一缓存；不能据此承诺持久化，也不能自行假设跨账号共享 |
| 文件上传显示完成 | 用正常业务入口重新取回，对照文件内容或摘要 | 检查取回与完整性；没有取回能力时不宣称完整性通过 |
| 管理页面隐藏了按钮 | 使用已授权的无权测试账号直接请求目标操作 | 检查该对象、该操作的授权；不是全面安全审计 |
| 内容发布显示成功 | 从适用的另一角色检查同一个内容对象 | 检查交付与可见性；另一个账号缺失时保留缺口 |

它不是机械给每个功能加“断网、杀进程、重试”全家桶。动作由业务风险和可用能力决定，先说明对应哪条要求、为什么值得测。没有相关性和安全条件的故障不执行。

**首版**：根据要求与现有工具选择独立观察位置、准确对象匹配，以及适用的重新进入、重新读取或冷启动；恢复验证在故障能力与授权具备时执行。不同执行方法不等价，例如冷启动、force-stop 和系统回收需分别限定结论。

**后续**：可控丢响应、重复请求、生命周期故障等精确探针。断网成功必须有观察记录，不能把点击了“断网”当成异常真的发生。

### 核心二：证据对账——每个结论都有对应事实

把“要求—场景—观察—结论”关联起来，避免用大量截图淹没关键缺口。

它还应告诉用户“现有绿灯缺少哪一项证明”。例如已有测试全部通过，但没有检查 App 新进程中的收藏列表；本次复用原有操作与数据，只补这项缺失观察。示例中的测试数量和结果由实际项目决定，不以新增测试条数作为产品成绩。

用户能展开一项结论看到：目标从哪里来、实际运行对象、适用角色或输入经过什么操作、核对的是哪个业务对象、证据位置、还有什么没看见。

例：发送方界面成功、接收方没有出现、后端记录存在，应显示三处事实及尚未确定的原因，不能凭任意一处成功给整体通过。

**首版**：结构化清单、工具原始结果引用、版本/对象关联、失效材料检查、保留每次重试。附带小型离线材料核验工具，检查引用与规则一致性；它不是能证明任意事实真实性的裁判。

**后续**：对已支持执行器进行更自动的记录采集、证据时间线和跨角色关联展示。

### 核心三：越用越省事——保存业务回归，而不只是聊天摘要

第一次验收产生可审查的回归包：业务约定、原生场景、数据准备、清理条件、已有测试入口及必要的新用例和运行说明。另一位未安装该 Skill 的执行者应能通过项目原生命令使用，明确环境前提与人工观察部分；尚未独立重跑验证的资产标为草案。下次复验重新获取事实，不复用旧结论。

后续变更到来时，指出哪些旧承诺可能受影响、哪些证据已经过期、建议先重跑什么。影响范围来自明确的需求/用例关联与代码线索，不把静态分析当作“遗漏概率为零”的证明。

**首版**：显式保存与复验、版本失效检查、已有测试复用，以及可脱离本次对话交接的原生资产；独立交接不以接入团队云或托管 CI 为前提。

**后续**：变更驱动的回归建议、验收约定差异、误报与接入成本的长期反馈。

### 增强一：缺口导航

缺少必要输入、测试身份、执行入口、观察手段或业务规则时，输出最小补充项及其影响。能安全执行的独立场景可以继续；缺少条件的原始目标仍留在报告中，不以工具能力改写用户请求。

### 增强二：有预算的风险选测

优先覆盖本次变更、核心用户承诺、已知缺陷和高风险失败路径。用户可以限制预算；未指定时采用并展示[默认执行预算与退出规则](ARCHITECTURE.md#执行预算与退出)，具体数值仅在架构文档维护。预算不足限制执行投入，不删除原始目标或降低通过标准；必测项没完成时整体不会得到完整通过。

## 4. 一个入口，三种意图

用户不需要理解内部数据格式，也不需要分别安装很多业务 Skill。一个 Skill 识别三种意图，均可用自然语言表达：

| 意图 | 用户说法示例 | 交付 |
| --- | --- | --- |
| 验收功能 | “上传修好了，验收一下中断恢复和最终文件。” | 本次约定、实际执行、结论和证据 |
| 复验回归 | “按上次的上传验收包，再测这个版本。” | 新运行与差异，旧事实不混入 |
| 只做计划 | “先看看这个功能该怎么验收，不要操作环境。” | 交付约定草案、能力缺口和执行计划后退出；不要求运行就绪，不产生通过结论 |

触发条件是功能验收、修复后的用户结果确认或真实流程回归意图，不要求用户使用固定术语。查询单元测试报错、代码审查、性能基准、视觉美化不应被强行接管；普通代码修改也不自动授权环境操作。自然触发和近似负例按[验证方案](VALIDATION.md)单独检验。

### 首次使用

1. 先列出原始验收目标及来源，识别验收、复验或计划意图。每个明确目标都要进入对应验收义务、明确缺口，或依据用户确认/其明确引用的适用条件记录排除理由；不能因工具不支持而消失。
2. 按本次流程只读检查相关文档、已有测试与工具信息，形成简短摘要：原始目标、当前能力、缺口、预计范围与预算。计划模式只依据现有材料标出待确认的运行条件；执行/复验再核查实际运行对象。不自动运行陌生脚本或安装依赖。
3. 计划模式在约定草案、场景、观察方式、执行前提和缺口已说明后退出；缺账号、设备或运行环境可作为待补前提，不阻止形成计划。执行/复验模式只对拟执行场景检查就绪，按实际能力接入。
4. 规则明确且操作在既有授权内，展示约定后继续；出现新的业务判断或敏感副作用时先确认。优先复用项目工具与习惯，profile 是可选缓存；准备、观察和清理使用场景所需的已授权方式，仅在需要后端事实时要求相应入口。

例如已有获授权的网页测试环境、测试身份和浏览器执行工具，没有 Skill 配置，也可以按实际能力准备上传取回场景；已有命令入口和结果文件的项目按对应方式接入。是否能形成有效结论仍取决于要求所需证据，运行后再按项目写入授权保存已验证的绑定。

用户要计划时，交付完整计划即完成该请求；用户要实际验收时，只有计划或部分可执行场景不能冒充完成。首屏列出原始目标中仍有核查缺口的部分；全部必需目标已有效判定时，可以是“验收已完成、发现业务失败”。产品的实际验收激活指标仍是一次有证据的运行。

### 日常使用

默认读取项目已审阅的配置和回归包，重新检查版本、环境与能力是否变化。不会每次重复问已确认的技术信息；账号、权限、业务规则或外部副作用发生变化时仍需重新处理。

## 5. 完整用户链路与结束条件

下表适用于执行/复验；计划模式按第 4 节交付后退出，不进入运行就绪、执行与环境处置步骤。

| 阶段 | 要做的工作 | 进入下一阶段的条件 |
| --- | --- | --- |
| 理解 | 还原用户、入口、结果、失败与恢复，保留全部原始目标 | 每个明确目标有义务、缺口或有用户依据的排除记录；争议明确可见 |
| 就绪检查 | 确认本场景需要的运行对象、工具、输入、适用角色、观察能力和权限 | 拟执行动作有可行且获授权的路径；缺能力的目标仍保留 |
| 锁定约定 | 确认原始目标与执行范围的关系、预期、证据义务、故障条件和预算 | 必测集合及必要运行标识固定，假设不冒充事实 |
| 准备 | 建立本场景需要的测试数据与初始状态，记录本次归属 | 前提可证明，失败处置路径已知；不统一要求账号、安装包或设备 |
| 执行 | 运行现成测试或调用工具，保存实际观察 | 步骤结果可定位；不明副作用先核查再继续 |
| 核验 | 按要求比较事实，进行相关的结果反证 | 通过有证据，失败有反例，材料不足保留未验证 |
| 结束处置 | 核对活动执行者；确认相关执行者终态并保全材料后，做最终清理与资源归还 | 处置已完成，或显式交接活动/未知执行及残留责任；尚未结束或无法确认时，不能声称环境已恢复 |
| 交付 | 展示原始目标的核查覆盖、业务判定、处置情况、证据与回归包状态 | 用户能核查结果和继续工作；移交的执行责任、材料与待办有明确接手方式 |

执行者停止与交接、材料保留和清理的具体约定见[架构与执行约定](ARCHITECTURE.md)。默认不自动改业务代码、不自动 push、不发公共评论。用户另行授权修复时，修复之后产生新的验收运行，不能覆写修复前证据。

## 6. 一份用户愿意看的报告

以下是**虚构的设计示例**，不是实际测试结果：

> 文件上传验收：发现失败，尚有未验证项。\
> 原请求：正常上传、离线后恢复、最终文件完整性、无权账号访问限制。\
> 本次运行版本已关联；前三项已核查，无权访问限制尚未验证。\
> 正常上传与取回：通过，文件内容一致。\
> 离线恢复：失败，重试后出现两个相同业务文件记录。\
> 无权账号访问：未验证，缺少该测试账号。\
> 下一步：先处理重复记录问题；提供无权测试账号后补验访问限制。\
> 结束处置：执行者已结束，本次测试数据已清理，证据保留在独立运行目录。回归包：草案已生成，尚需完成从干净环境的再次运行。

报告分开呈现业务判定、验收运行完成度和环境处置；同时展示原始目标中哪些已核查、哪些仍有缺口。有效 FAIL 表示业务不满足，不自动表示验收未完成；漏目标、缺观察或其他 Gap 才造成核查未完成。详情展开要求、运行对象、尝试和证据，排除项显示用户依据。不存在“总体 95 分所以可上线”的自动结论。

三类输出：给人看的简报；给工具读取的结构化运行材料；供下次使用的回归包。原始凭据、登录态和真实用户数据不进入公开报告。

## 7. 首版支持面与有意不做

### 当前建议范围

- 一个 Skill；现有 Agent 承担推理与调用，不增加常驻服务或模型账户。
- 以本次用户流程所需执行与观察能力接入，复用现有浏览器、项目测试、接口/命令或移动端工具；技术栈无关不等于这些组合已全部支持，实际支持声明以 [L3 真实工具链证据](VALIDATION.md)限定。
- 持久化、文件完整性、跨角色可见性与恢复等属于候选验收方向，按实际需求取用；Flutter/Android 与 Maestro 只可作为其中一组样本条件。
- 优先测试环境，角色与数据范围按要求和实际条件确定。业务对象授权需要能建立权限关系的测试身份与观察能力；系统权限依据实际平台状态与任务恢复验证。只有设备相关要求才需要适用真机证据，界面、接口或命令的结果分别限定范围。
- 复用项目现有的准备、观察与清理方式，不构建通用 fixture 平台。
- 简明 Markdown 报告、结构化证据索引、可独立交接的原生测试及运行说明；标明未自动化的观察。HTML 查看器不作为首条价值验证的前置条件。
- 小型材料核验与报告脚本；支持的工具结果做明确映射，不接受任意日志都“智能判定通过”。首个受控实现样本验证报告/材料核验，不强制执行三条命令；其他调用缺该工具时，按[业务判定与材料核验状态](ARCHITECTURE.md#业务判定与材料核验状态)区分有依据的人工判定与尚未执行的机械核验。统一复制证据按实际需要增加。

### 后续增强

更好的自动采集、精确故障注入、基于变更的回归建议、更多经验证的工具组合，以及团队云端协作与高级 CI 编排。项目原生测试和独立运行说明属于首版交接目标，不随这些增强一起延后。每项增强先有真实任务，再决定实现。

### 暂不做

通用动作 DSL、云设备调度平台、自动探索整个产品、自动修复业务代码、任意站点账号自动登录、生产环境破坏性测试、全自动发布批准、全平台一装通用承诺、硬件真实性判断。

**设计可扩展不等于首版全部实现。** 一旦功能主要变成运行环境运维，或用户要学习另一套大型测试语言，应回到 Skill 的定位。

## 8. 产品成立的基准

最强基线不是“什么都不用”，而是同一个宿主 Agent、同样工具、同样验收要求和预算，在没有本 Skill 时完成同一任务。

产品需要同时满足：

- **有用**：真实缺陷被发现，健康流程也能正确通过；不能一律“未知”来换取不出错。
- **可信**：缺证据、错版本、错账号、无效故障和重试后成功不会被洗成完整通过。
- **省事**：把首次接入、人工纠错和维护都算入成本；不能只展示 Agent 操作时间。
- **可复用**：两个不同项目能共用主要方法与材料工具，至少两位非作者开发者主动完成第二次验收。

建议目标为人工时间中位数降低至少一半；这是需验证的产品门槛，不是已取得的成绩。具体测量和失败样本见 [验证方案](VALIDATION.md)。

## 9. 开发顺序：先纵向闭环，再扩展能力

| 阶段 | 工作内容 | 退出条件 |
| --- | --- | --- |
| 0：样本和约定 | 按已有授权、环境与工具条件选择具体样本，准备健康、已知缺陷和条件不足对照，记录基线 | 业务标准明确、结果可核查、环境影响受控；不要求某种技术栈或设备 |
| 1：最小 Skill | 精简流程说明、按需参考、一个非 IM 用户流程、最小材料核验 | 健康结果有效通过；已知缺陷被检出；条件不足保留原始目标并正确退出 |
| 2：可信与复验 | 运行对象绑定、重试保留、缺口处理、原生回归包、执行者与材料交接 | 从干净状态独立复验；坏材料和错误运行对象不能换来通过；清理与中断责任明确 |
| 3：第二个业务与项目 | 在另一实际项目复用，可采用不同执行形态，按目标补跨角色等必要验证 | 复用不依赖作者临时补大量隐形脚本；每项支持声明有对应 L3 证据；同一项目换入口不能代替跨项目复用 |
| 4：对照与试用 | 自然触发与近似负例、有/无 Skill 配对评测、故障样本、真人复用 | 达到验证门槛，公开记录实际支持组合和限制后再发布可运行 Skill |

研发安排依样本与阶段退出条件确定；限定探索时间不能替代真实验证，也不构成完成所有执行形态与外部试用的期限承诺。

## 10. 样本选择与待验证事项

1. **定位已定**：用户流程与验收方法面向不同技术栈；先核对原始目标，再判断能力。实际支持组合由真实执行证据逐项建立。
2. **首个样本待选**：按已有条件选择最容易形成健康、缺陷与不足对照的流程；分页导出完整性、收藏持久化、上传取回都只是候选。样本项目与获授权的测试环境尚待选择，仓库公开不授权访问任意产品数据。
3. **安全约束保留**：测试环境优先、验收与修复分离、无默认发布权限。已授权环境内的常规测试无需每一步重复确认，新增敏感操作仍需对应授权。

仓库名称为 `functional-acceptance`。开源许可与安装渠道尚未确定；当前仅发布设计基线，没有可安装 Skill、软件发行版或生产可用性声明。
