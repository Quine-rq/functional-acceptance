# 本轮功能加固结果

日期：2026-09-08。起点：`5ec62872eaef6509058b88dded031664beb940d1`。

**结论：已经有可运行、可复验的开发预览，但还不能称为成熟产品。**
本轮补齐了真实项目回归资产、失败分类、恢复用例和带原始证据的行为对照。
没有把“安装成功”“命令成功”“找到了一个 Bug”合并成产品已成熟的结论。

## 用户实际得到什么

| 原先的障碍 | 本轮变化 | 证明与边界 |
| --- | --- | --- |
| 已有测试全绿，仍可能漏验用户要求 | Skill 要求把每项结果映射到实际断言，缺少的检查单列 | 有无 Skill 的原生对照都识别了绿色 smoke 遗漏的末页；不能据此称 Skill 独有优势 |
| 真实项目流程依赖作者临时修脚本 | 新增独立运行的 linkding 原生包、固定版本、准备步骤和完整证据快照 | 独立代理能按指南交接；不是外部人类试用 |
| 测试程序出错容易被当成产品 Bug | 区分业务反例、观察/测试故障、环境缺口和交付失败 | 真实备注丢失是 FAIL，缺观察是 UNVERIFIED，启动失败不是业务失败 |
| 重登、重启、响应丢失之后是否可靠不清楚 | 核对同一对象、独立数据库和旧数据基线，避免不确定写入后盲重试 | 仅覆盖这个固定版本的本地流程，不外推为所有弱网、并发或移动端行为 |
| 中断时只相信 finally | 修正同步浏览器调用内直接抛中断异常的做法 | 原失败 130 秒后被外层终止；修正后同一中断探针约 6.2 秒退出，保留 UNVERIFIED 与清理记录 |
| 弹窗可见就立即操作，可能早于导航完成 | 回归脚本在点击前监听对应导航的完成事件，再操作详情 | 受控延迟真实导航回调时，旧等待方式 3/3 轮出现确认框被缓存事件移除；这不是原始偶发故障的唯一根因证明，也不是上游快速点击体验已修复 |

入口：[Skill](skills/functional-acceptance/SKILL.md)、
[原生工作流参考](skills/functional-acceptance/references/native-workflow.md)、
[linkding 运行指南](examples/linkding/README.md)。Skill 仍只有六个分发文件；
没有加入新的 Agent 运行时、通用适配器或安装器。

## 做过的实测

- 真实浏览器 → Django → SQLite：正常流程、运行时注入的备注丢失、缺少必要观察、
  会话丢失、POST 已成功但响应丢失、Unicode 多行备注维护练习。
- 安全与交付：原文件拒覆盖、敏感调试页不截图、启动日志限量与已知测试密钥脱敏、
  执行脚本快照、环境变化检查、SIGTERM、报告目标冲突、端口被占用。
- Codex：当前版本有/无 Skill 各五次（漏页各三次、计划各一次、历史核查各一次），
  另保留旧版 Skill 一次；实际轨迹、CSV、文件摘要、时间和原生 usage 均保留。
- 安装包：五种目标安装/重装/卸载的十项检查；这不等于五种 Agent 都已经执行验收。

导航等待调整后，独立执行者已对当前候选将六种完整场景各跑一次：四个正常/恢复场景
为 PASS，注入备注丢失为预期 FAIL，缺少必要观察为预期 UNVERIFIED。未自动重试。
同一精确时序注入对照从旧方式 3/3 轮出现确认框消失，变为新等待方式 3/3 轮保持可用；
这验证的是特定调度窗口，不证明已消除一切偶发问题。之前的失败书签和记录都保留，未事后清空。

原始失败没有被“最后一次通过”覆盖。完整逐次记录、脚本身份、评分和限制见
[评估记录](evals/results/2026-09-08-product-hardening/README.md)。
原来的 [linkding 研究](evals/results/2026-09-08-linkding/README.md)保持不变。

## 对照没有证明产品已经胜出

两组都在三次漏页试验中找到了同一个缺陷，完整满足全部评估项的记录均为 **4/5**。
两组各有一次未实测已有文件冲突：Skill 组明确说明未验证，无 Skill 组漏报了这项缺口。
当前 Skill 组共 22/23 个断言通过，无 Skill 组 21/23；这种小样本差异不能解释为真实收益。

当前 Skill 实际加载了 **4/5** 次；历史核查那次只列出了文件路径。
两组历史核查均没有重新运行应用或生成项目文件，但其中一个正确回答不能归功于未加载的 Skill。
文件清单不含 `.git`、`__pycache__` 和空目录，不能声称整个宿主绝对零写入。

平均原生执行墙钟时间约 52.6 秒与 46.5 秒。保留了真实 token 计数，未伪造货币成本。
宿主继承的全局配置未隔离、模型版本未在轨迹中绑定、部分运行同时进行，且没有人工工时实验，
因此这些时间和 token 数不支持“更快、更便宜”的因果结论。

## 本地检查与发行状态

本轮最终本地检查：89 项 helper/collector/package/native-pack 测试，
23 项独立导出回归，10 项安装集成检查。它们检查不同层次，不能换算成产品成熟度分数。
实际浏览器环境为 macOS、Python 3.14.4、Node 22.22.3、Chrome 152.0.7977.76；
linkding 使用固定提交及锁定依赖。最初通过环境查找未发现 Python 3.10，随后在已有
工作目录找到 3.10.20，并用它重新执行当前 helper/native-pack 单测及独立导出回归；
这不代表 linkding 也支持该解释器。新提交的远端 CI 尚未运行。

本轮只做本地提交；没有新的 push 指令，不推送、不发版、不修改公开仓库设置。

## 仍需跨过的门槛

1. **第二种宿主真正执行成功。** Claude Code 2.1.220 的两次尝试都在任务执行前遇到
   认证 403。没有迁移或转发凭据；需要恢复该宿主的可用授权后，重新验证实际行为。
2. **外部开发者的真实收益。** 代理交接不能代替真人。已备好
   [外部试用协议](evals/HUMAN-PILOT.md)，尚未招募或虚构参与者。
3. **更广的稳定性证据。** 还缺多项目、不同缺陷和正确对照下的重复行为验证，
   安装升级中断恢复、全触发矩阵，以及未覆盖平台。独立复跑在清理控制书签时出现过一次
   超时：会话恢复已通过，但控制对象仍留存。补充逐动作与私有异常诊断后，会话恢复和
   响应丢失各通过一次；Unicode 场景又在点击 Confirm 时超时，确认按钮在可见断言后
   消失，目标和控制对象均留存。随后取得受控时序反例并增加导航完成等待，当前六场复验
   均符合预期；但单场复验与三轮受控对照不足以证明原来的所有偶发失败都已消除，
   更不能把等待导航后的稳定流程等同于上游快速点击体验已修复。当前 native 包也不是通用执行引擎。
4. **发行条件。** 许可证等待维护者决定；私密安全反馈渠道、支持承诺和正式发版权限仍需确定。

下一步不应为了包装而增加更多功能名词。应先让外部开发者在自己的项目中完成一次真实验收，
再用留下的回归检查承接一次需求变更，量出发现问题、节省返工及额外维护的真实成本。
