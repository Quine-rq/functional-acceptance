# 本轮交付：升级保护、旧材料兼容与重复回归

2026-09-08，起点 `ac8eda6`。**本地可靠性工作已有可复验交付；陌生项目
无人指导接入、多宿主执行和真人收益仍未验证。不能称为成熟产品。**

## 用户实际得到什么

| 原先的问题 | 本轮交付 | 验证范围 |
| --- | --- | --- |
| 重装会覆盖本地修改，中断可能留下半份 Skill | 只读升级对比工具，暂存安装、备份及恢复指南 | 真实固定版本安装器、两种目录布局、复制中途 SIGKILL；不承诺原子升级或自动合并锁文件 |
| 文件内容没变，也可能已经无法执行 | 精确比较所有者/组/其他用户的执行位 | 独立审查用 `0755 → 0655` 复现 EACCES 和漏检；同一反例修正后被识别，原失败记录保留 |
| 升级后是否仍能读取 M1 材料不清楚 | 固定的旧材料兼容测试 | 原 `edc295c` 采集器生成的三种新合成运行，由当前安装后的 helper 原样重查；未知格式和缺失旧证据不通过 |
| 浏览器流程只跑一遍，恢复是否稳定不清楚 | 三轮正常/会话失效/响应丢失/Unicode 回归及故障控制 | 十二场完整流程 PASS，另四项控制符合预期；同一已准备环境，不是普遍稳定率 |
| 找到一个问题后，其他明确要求可能没测 | Skill 增加缺项执行安排与交付前逐项对账 | 指令已修改；候选版原生行为评估未执行，不能说漏验已经消除 |

入口：[升级指南](UPGRADING.md)、[只读对比工具](integration/review-update.mjs)、
[旧材料说明](tests/fixtures/m1-history/README.md)、[完整本轮记录](evals/results/2026-09-08-reuse/README.md)。
分发给 Agent 的 Skill 仍是六个文件；新对比工具和故障探针只在开发目录，
没有加入新的 Agent 运行时或通用安装器。

## 验收与失败保留

- 当前 Python 本地检查：**93 项** helper/collector/package/native-pack 测试，
  **23 项**独立导出测试，在 Python 3.10.20 和 3.14.4 各跑一遍。
- Node 22.22.3：最终 **19 项**安装/升级集成测试；独立升级验收先是 8/9，
  抓到所有者执行位漏检，修正后对原样保留的反例复验为 9/9。
- 新兼容测试最初错误地期待无效格式响应含 `qualified_pass`，而当前约定
  返回 `material_status: invalid` 且没有业务判定。读实现后修正的是测试断言，
  未修改 helper 的格式或错误语义。记录核查器也修正过 schedule 外层结构读取。
- 浏览器共保留 **17 次 native 尝试**：先有一次权限失败，随后在正常权限下
  执行完整的 16 项固定清单。前次的端口拒绝和记录器 `ps EPERM` 没有被覆写。
- 正常与恢复流程 12/12 PASS；注入备注丢失为 FAIL，缺观察、中断、端口
  占用为 UNVERIFIED。67 次浏览器启动对应 67 次关闭，最终已记录自有进程
  和端口均确认结束。**不是 16 个业务 PASS。**
- 原书签 3、12 没变，故障书签 47 留存；标签从 15 增至 29，登录会话按
  既有策略保留。没有清空数据库、删除历史失败或声称环境绝对零写入。

上述测试数量来自不同层次，不折算为成熟度分数。摘要、原生事件、执行脚本、
实际 CSV、评分及源/公开副本摘要均可核查；私有数据和原始诊断不随仓库发布。

## 按要求跳过、没有记为完成的部分

1. **新的原生 Agent 对照。** 计划十二次，只有旧版显式调用实际完成；独立
   评分六项均满足，包括真实同名文件拒覆盖。候选调用的正常审批被拒绝，
   另外十一次没有执行，零组完整配对。没有为缺失试验补零分、时间或 token。
2. **陌生项目接入及后续维护。** 固定 sqlite-utils 源码及依赖已准备，没有
   预写验收脚本。两次原生宿主都在初始化时退出，无模型轨迹、生成测试或
   可交接资产；因此无法进行该项目的无 Skill 独立重放和下一次需求变更维护。
   没有拿作者另写脚本代替自主接入证明。
3. **第二宿主。** 本轮 Claude 没有启动；之前的认证 403 仍未解决。没有更改
   认证、模型、服务地址或安全设置，也没有换渠道绕过审批。
4. **真人、许可及发布。** 真人试用、许可证选择、私密安全联系渠道和正式
   发版仍需维护者参与。没有假参与者、估算成实际工时或未经授权的公开动作。

已单独询问本轮原生评估的传输授权；未得到答复前继续跳过该依赖。
skill-creator 的评估流程保留了全部未运行项和可查看结果；writing-for-agents
帮助把改动限定为两处具体完成条件，未堆叠冗余指令。尚无真人反馈。

## Git 与 CI

起点 `ac8eda6` 的 [远端 CI](https://github.com/Quine-rq/functional-acceptance/actions/runs/34197483133)
三个任务均成功。当前新增检查已接入工作流，但本轮尚未 push，新的远端 CI
没有运行。本地提交不代表远端同步，更不代表正式发行。

下一次最有价值的工作是解除上述原生评估权限缺口，完成冻结的陌生项目任务
及其真实维护交接，而不是继续增加自制示例或宣传适配数量。
