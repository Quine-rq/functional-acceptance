# 原生评估与真实项目交接

本轮补上了此前被权限和认证阻断的原生执行，并修正了真实回归包的进程残留问题。**还不能称为成熟产品：最终 Skill 仍有漏验样本，第二宿主只验证了受限历史审核，尚无外部真人收益证据。**

## 用户实际得到什么

| 改前的问题 | 本轮处理 | 证明与限制 |
| --- | --- | --- |
| 审核历史记录时，助手可能声称“补一个 CSV 就足以证明所有要求” | 将历史审核独立成分支，逐项区分完整输出、历史期望输入、新文件创建及全部既有文件的前后状态 | Claude 最新两次明确读取 Skill 后达到冻结的窄口径要求；较早修订仍失败，全部保留。没有健康历史对照，不证明自然加载或全面可靠 |
| 首个业务问题被发现后，其他独立检查可能被遗漏 | 将逐项执行要求放到执行阶段，并要求使用本次临时对象检查危险失败分支 | 最终 Codex 候选仍有一次没测已有输出拒绝覆盖，也未说明缺口原因。指令改进不能当作消除了漏验 |
| 陌生项目中生成的 pytest 包，正常通过却可能在超时后继续写入 | 固定进程组归属、启动即记录 PID、TERM/KILL/wait 并核对终态；未知时保留证据、不自动重试 | 原始包真实 pytest 故障注入后写入者仍活；同反例在修正包中停止。迁移后真实 SQLite 流程 5/5；两版 Python 各 11 项生命周期回归通过 |
| 插件检查晚于应用导入，拒绝之前插件已经执行；同名包也可能抢占导入 | 插件及发行元数据在任何应用导入前校验；本地包使用显式 `__init__.py` | 固定真实上游加仅写临时标记的合成插件：旧探针执行插件，新探针拒绝且未执行，无插件正常通过；另有 3 项身份回归和同名包 RED/GREEN |

可直接查看 [sqlite-utils 接入包](examples/sqlite_utils/README.md)、[当前 Skill](skills/functional-acceptance/SKILL.md)和[详细证据索引](evals/results/2026-09-08-native-followup/README.md)。进程组支持只属于这个原生示例，没有给 Skill 加一个通用运行时。

## 原生 Agent 评估

Codex 使用相同的冻结需求、故意漏掉末页的合成导出程序、工具和时间预算比较新旧 Skill。共保留 38 次原生调用，版本分别计分，不混在一个通过率中：

| 修订 | 完成范围 | 候选 / 基线断言满足数 |
| --- | --- | --- |
| 原 `7d94bb6` | 六对，基线 `ac8eda6` | 24/25 / 25/25 |
| V1 | 仅显式请求一对，后续五对未启动 | 6/6 / 6/6 |
| V2 中间版 | 六对，基线 `7d94bb6` | 25/25 / 25/25 |
| V3 最终版 | 六对，基线 `7d94bb6` | 24/25 / 25/25 |

不能据此声称候选更好。V3 双方都只在三个执行场景中的两个实测了已有输出拒绝；基线的一次明确说明保守解释 README 后未测，满足冻结的“有理由标记未验证”评分，而候选的一次没有披露缺口。因此 25/25 不等于所有分支已经实测。后续成功不覆盖原始失败。

Claude Code 2.1.220 使用 Sonnet 4.6 和官方 Anthropic 端点完成 10 次调用，客户端累计估费 **$0.4295049**，低于本轮授权的 $5；这是 CLI 报告，不是账单核验。两次 slash 请求没有充分的 Skill 加载证据；由于宿主可能先展开命令，不能仅凭没有 Read 事件断定未加载。其余八次显式要求读取完整 Skill，只证明这种调用方式下的受限只读表现。

V3 最后两次各满足 4/4 条冻结断言，但评分并不要求完整证据采集计划；编码证明细节仍不完整，另有一次目录内容说法缺成功观察支撑。这不是“回答无瑕疵”，也不是在 Claude 中执行完整用户流程的证明。保留这些窄口径与失败，比扩大成功标签更重要。

原生 traces 未独立绑定 Codex 实际模型名，记录 `actual_model=null`，不把配置值冒充实测模型。费用与 native token 数据也不等于人类劳动、账单或节省时间。

## sqlite-utils：生成、重跑、维护、硬化分别核对

上游固定为 `85b1be10c81d9dd3567e36faf8dd411e4a8789bd`。两名原生执行者面对不含预制验收脚本的源码，分别生成可运行包；之后两个新的执行者分别在没有 Skill 的副本中重跑和维护候选包。维护需求是协调者预先确定的合成字段变更：保留邮编前导零，以及 null 与空字符串区别，不是假装外部反馈。

候选生成、旧版生成、无 Skill 重跑、维护四个已完成原生调用合计 1,657,921 个 input+output token；缓存 token 已包含在 input 中，未重复相加。另有被主动中止的初始化尝试，其用量未知，不算进这个总数。依赖环境预先准备，没有证明从零安装或长期省时。

原包的正常业务通过不涵盖超时安全。后续审查发现：外层只杀 pytest，内层计时和终态记录又依赖 pytest，导致 CLI 可能继续写入且没有启动 PID 证据。修正同时覆盖进程归属与记录时机。原生 RED/GREEN 保留相同故障注入，5 项业务断言未放宽。

集成回归另外发现测试自身的启动竞态：500 毫秒外层期限会在较慢解释器启动期间选中“超时”，掩盖要测的“父退出但子仍活”；内层缩短的 300 毫秒期限也存在类似干扰。独跑曾通过；单变量慢启动探针及失败测试验证了时间窗口。测试现在独立等待就绪、给父退出场景合理的有界启动时间，并分别增加慢启动回归，业务计时与断言不变。原失败和探针同样保留。

## 本地验证与边界

- Python 3.10.20 和 3.14.4：各 93 项 helper/collector/package、23 项独立导出、11 项生命周期、3 项身份测试通过；共 130 个不同测试方法，不把双版本执行数称为不同功能。
- Node 22.22.3：19 项安装和升级测试通过。
- 集成后的独立 sqlite-utils 副本：真实 pytest 5/5，读取实际 JSON 与 SQLite、每条命令终态和进程组清理状态。
- 旧保留记录核查通过；新记录的核查器检查摘要、原始输出副本与清单一致性，不调用模型，不把历史材料变成实时验证。

POSIX 组回收不保证 runner 被 SIGKILL、主机失效或子进程主动脱离会话后的清理。磁盘耗尽、并发写入、崩溃恢复、真实移动端与生产系统仍未验证。

## 下一步，按缺口推进

1. 针对漏验做预先冻结的覆盖清单实验，检验完整性改善是否稳定，同时保留预算和基线；不要无限追加相似提示词。
2. 为 Claude 补健康历史对照与真实执行流程，随后验证自然发现，而不是把显式 Read 成功当作全部接入完成。
3. 让外部开发者在自己的项目从零接入，记录真实阻碍、人工修正和第二次维护成本。没有真人参与就保持未完成。
4. 由维护者决定许可证、安全支持和发布范围。当前授权只涵盖本轮检查和 origin/main 推送，不自动批准正式发布。

本轮使用 skill-creator 的独立评分与标准查看器保留失败，writing-for-agents 帮助将历史审核放到正确分支，diagnosing-bugs 用同反例验证进程修正。没有用文档检查代替真实执行。
